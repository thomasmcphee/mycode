#!/usr/bin/env python3

hostname = input("what value should we set for hostname?")

if hostname.lower() == "mtg":
    print("THe hostname was foudn to be mtg")
    print("hostname matches expected config")

    print("Exiting the script")


